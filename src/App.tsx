import { useState } from "react";
import { Button } from "./components/ui/button";

// import { supabase } from "./lib/supabase";

function App() {
  const [count, setCount] = useState(0);
  return (
    <>
      <div className="text-lg text-center pt-4">JP</div>
    </>
  );
}

export default App;
